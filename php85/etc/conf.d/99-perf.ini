; Performance tuning.
;
; realpath_cache holds resolved paths so workers don't re-stat large framework
; trees on every request. 4M comfortably holds a big tree.
realpath_cache_size=4M
realpath_cache_ttl=600
