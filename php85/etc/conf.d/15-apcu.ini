; APCu — user cache in shared memory.
extension=apcu.so

apc.enabled=1
; DEV: 1 — so CLI scripts / `php -S` get APCu too (set 0 in production).
apc.enable_cli=1

apc.shm_size=64M
apc.ttl=0
apc.gc_ttl=3600
apc.serializer=php
